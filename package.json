{
  "scripts": {
    "dev": "react-scripts start",
    "start": "serve -s build",
    "build": "cd backend && npm install && cd ../dots && npm install && npm run build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "heroku-postbuild": "npm run build"
  },
  "engines": {
    "node": "16.x"
  },
  "dependencies": {
    "serve": "^14.1.2"
  }
}
